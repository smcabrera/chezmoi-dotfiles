# Ship it!
- Create a commit using the commit command if any files are new or dirty in git
- Push to github `git push`
- If we don't already have a pull request create one with the following steps:
- Generate a description using the claude command `/create_pr_description`
- Use that description as an argument to create-my-pr, e.g. `create-my-pr "My PR description"`
